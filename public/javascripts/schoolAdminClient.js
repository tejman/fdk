// Generated by CoffeeScript 1.7.1
(function() {
  $(function() {

    /*
    ***************** Function Definitions **************
     */
    var showCurrentInfo;
    showCurrentInfo = function(userSchool) {
      var $inputLabels, label, name, _i, _len, _results;
      $inputLabels = $(".control-label");
      _results = [];
      for (_i = 0, _len = $inputLabels.length; _i < _len; _i++) {
        label = $inputLabels[_i];
        name = $(label).attr("for");
        if (userSchool[name] && userSchool[name].length) {
          _results.push($("#" + name).val(userSchool[name]));
        } else {
          _results.push(void 0);
        }
      }
      return _results;
    };

    /*
    ***************** Main Code **************
     */
    console.log(bootData);
    if (bootData.userSchool) {
      return showCurrentInfo(bootData.userSchool);
    }

    /*
    ***************** Click Handlers **************
     */
  });

}).call(this);
